version: 2.1

orbs:
  codecov: codecov/codecov@1.0.2 # need to allow 3rd party orbs in project settings on circleci
  python: circleci/python@0.2.1
jobs:
  build:
    working_directory: /root/project/CFD2021Projects   # Clone into a directory whose name matches your Package.
    docker:
      - image: julia:1.1                # image comes from Docker hub
    steps:
      - checkout
      - run:
          name: Install and Test this package
          command: julia -e  'using Pkg; Pkg.update(); Pkg.add(pwd()); Pkg.build("CFD2021Projects"); Pkg.test("CFD2021Projects", coverage=true);'
      - run:
          name: Process code-coverage
          command: julia -e 'using Pkg; cd(Pkg.dir("CFD2021Projects")); Pkg.add("CFD2021Projects"); using Coverage; coverage = process_folder(); LCOV.writefile("coverage-lcov.info", coverage)'
      - codecov/upload:
          file: "coverage-lcov.info"
          token: ${{CODECOV_TOKEN}}

# version: 2.1

# orbs:
#   python: circleci/python@0.2.1

# jobs:
#   build-and-test:
#     executor: python/default
#     steps:
#       - checkout
#       - python/load-cache
#       - python/install-deps
#       - python/save-cache
#       - run:
#           command: ./manage.py test
#           name: Test

# workflows:
#   main:
#     jobs:
#       - build-and-test
